Database folder

Contains DB-related helpers and migration scripts.

- `init_db.py` (root) initializes the SQLite DB for this prototype.
- For production, move migration scripts here and add SQL/DDL files and TimescaleDB migrations.
